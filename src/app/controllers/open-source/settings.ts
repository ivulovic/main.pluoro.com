import { OpenSourceScope } from '@implementation/open-source/settings';

export const settings = {
  [OpenSourceScope.Sepa]: {
    id: OpenSourceScope.Sepa,
  },
  [OpenSourceScope.Covid]: {
    id: OpenSourceScope.Covid,
  },
};

export default settings;
